---
import MainLayout from "./MainLayout.astro";
const { frontmatter } = Astro.props;
---

<MainLayout pageTitle={frontmatter.title}>
    <h1 class="text-xl italic" transition:name={`post-title-${Astro.url.pathname}`}>{frontmatter.title}</h1>
    <slot />
</MainLayout>